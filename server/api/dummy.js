// export default defineEventHandler((event) => {
//   // handle query params
//   const { name } = useQuery(event);
//   //accept query parameters

//   return {
//     message: `Hello ${name}`,
//   };
// });
